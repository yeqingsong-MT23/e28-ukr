<!-- UKAI_METADATA
{"code":"UK207","family":"UK200","script_name":"uk207.py","file_prefixes":["UK207"],"output_name":"UK207｜不同报价方式转化漏斗.xlsx","purpose":"按报价方式输出漏斗，并细分阶段内取消类型。","dimensions":["Month","Pricing_Mode"],"metrics":["Submitted_QPO_Count","Success_QPO_Count","In_Progress_QPO_Count","Cancel_QPO_Count","Success_Rate","In_Progress_Rate","Cancel_Rate","Initial_Stage_WootCancel_QPO_Count","Initial_Stage_SellerCancel_QPO_Count"],"filters":["Pricing_Mode 非空","阶段按报价字段存在性推导","取消拆为 WootCancel / SellerCancel","补 ALL 汇总"],"source_fields":["Submission_Date","QPO_ID","QPO_Status","Pricing_Mode","Initial_Quote","Second_Quote","MVM_Approved_Price"],"tags":["报价方式","Pricing_Mode","取消拆分","WootCancel","SellerCancel","阶段漏斗"]}
-->
# UK207 代码生成提示词

## 0. Prompt / 给 uk_ai_brief.py 的指令

你现在要生成一个**实际落地、可直接运行、会输出 Excel 报表**的 Python 脚本，目标文件是 `uk207.py`。

- 目标输出文件：`UK207｜不同报价方式转化漏斗.xlsx`
- 报表目的：按报价方式输出漏斗，并细分阶段内取消类型
- 代码风格必须对齐现有正式脚本，输出结果要像 `uk101.py` 这种可直接落地的 Excel 生成脚本
- 必须生成完整代码，不要只输出思路、伪代码、占位函数或半成品
- 必须包含 `parse_args`、阶段归并、取消类型拆分、`build_notes_rows`、`write_workbook`、`main`
- 必须默认自动识别对应基础表族的最新输入文件，并支持 `--input` / `--output`
- Excel 必须包含双表头、格式化列宽、冻结窗格、Notes sheet
- 下方各章节不是阅读说明，而是必须落地到代码中的业务规则与实现约束

## 1. 业务目标

从 `UK200` 基础表中，回答 **“不同报价方式的漏斗效果是否不同，以及取消是平台取消还是卖家取消”**。

## 2. 基础文件与数据粒度

- 基础文件：`UK200` 开头的转化漏斗 KPI 基础表
- 原始粒度：QPO 过程记录
- 输出粒度：`Month + Pricing_Mode`

## 3. 核心分析角度

1. **报价策略角度**：不同 `Pricing_Mode` 的转化表现
2. **结果角度**：提交、成功、跟进中、取消
3. **取消归因角度**：`WootCancel / SellerCancel`
4. **阶段角度**：不同报价阶段下的取消差异

## 4. 关键维度

- `Month`
- `Pricing_Mode`

## 5. 核心指标与口径

- `Submitted_QPO_Count`
- `Success_QPO_Count`
- `In_Progress_QPO_Count`
- `Cancel_QPO_Count`
- `Success_Rate`
- `In_Progress_Rate`
- `Cancel_Rate`
- `Initial_Stage_WootCancel_QPO_Count`
- `Initial_Stage_SellerCancel_QPO_Count`

## 6. 漏斗 / 阶段 / 状态逻辑

- 先按 QPO 归并漏斗结果
- 阶段仍由报价字段推导
- 取消要继续拆成平台取消与卖家取消

## 7. Excel 输出结构

- 主表：`PricingMode_Funnel`
- Notes：报价方式取值说明、取消拆分规则

## 8. 实现提示

- `Pricing_Mode` 非空
- 必须补 `ALL` 汇总
- 可附带输出识别到的报价方式列表，方便检查脏值

## 9. 代码复刻约束

以下内容直接从当前正式脚本反推，属于生成代码时应优先保持一致的实现约束。

- 默认输出文件名：`UK207｜不同报价方式转化漏斗.xlsx`
- 现有脚本实际创建的 Sheet：`UK207_PricingMode_Funnel`, `Notes`
- 现有脚本核心函数：`parse_args`, `aggregate`, `write_workbook`, `main`

## 10. Notes Sheet 输出要求（反推后沉淀为基准）

### 10.1 字段口径说明区

- 当前正式脚本的工作簿实际包含 `UK207_PricingMode_Funnel` 与 `Notes` 两个 Sheet。
- `Notes` 实际采用 6 列结构，先写规则区，再写 QA 项：`Field (EN)` / `字段中文` / `Field Group` / `Business Meaning` / `Calculation / Rule` / `Notes`。
- 文档必须明确以下真实实现：
  - `Pricing_Mode` 为空的记录不进入结果表
  - 主表包含 `Month=ALL, Pricing_Mode=各报价方式` 以及 `Month=ALL, Pricing_Mode=ALL` 两类汇总行
  - 总体 `Success_Rate / In_Progress_Rate / Cancel_Rate` 分母为 `Submitted_QPO_Count`
  - 阶段内 `*_Submit_Rate` 分母为 `Submitted_QPO_Count`
  - `*_Stage_Rate` 分母为对应阶段 `Stage_QPO_Count`
  - `*_Cancel_Rate` 分母为对应阶段 `Stage_Cancel_QPO_Count`
  - 取消拆分为 `WootCancel` 与 `SellerCancel`，且 `Cancel = WootCancel + SellerCancel`
  - 存在异常成交整单剔除规则：未进入任何报价阶段却标记为 Success 的 QPO 全部剔除。

### 10.2 运行元信息区

- 当前 `Notes` 已写入 QA 项，至少要保留：`QA_Input_Rows`、`QA_Valid_Rows`、`QA_Excluded_QPOs`、`QA_Final_QPOs`。
- 同时要记录：状态映射、报价进入标记规则、阶段归属规则、汇总行规则。
- 这一区域的目标是让后续重建脚本时，既能解释报价方式漏斗，也能追踪异常整单剔除规模。

### 10.3 最终要求

- `UK207.md` 必须把“报价方式主表 + 取消拆分规则 + QA 区”一起固化下来。
- 如果 `Pricing_Mode` 过滤、阶段分母、取消拆分或异常剔除规则发生变化，必须优先更新本文件。

