<!-- UKAI_METADATA
{"code":"UK205","family":"UK200","script_name":"uk205.py","file_prefixes":["UK205"],"output_name":"UK205_KP UEA转化漏斗概览.xlsx","purpose":"按 KP 跟进人输出月度漏斗与节点取消分布。","dimensions":["KP_Flup","Stat_Month"],"metrics":["Total_QPO_Count","Success_QPO_Count","Cancel_QPO_Count","InProgress_QPO_Count","Success_QPO_Rate","Cancel_QPO_Rate","InProgress_QPO_Rate","Node distribution"],"filters":["KP_Flup 必填","默认仅统计 2025-01-01 之后","支持 ALL 汇总"],"source_fields":["Submission_Date","QPO_ID","QPO_Status","KP_Flup","WPO_Current_Node"],"tags":["KP漏斗","跟进人","节点取消分布","月度汇总"]}
-->
# UK205 代码生成提示词

## 0. Prompt / 给 uk_ai_brief.py 的指令

你现在要生成一个**实际落地、可直接运行、会输出 Excel 报表**的 Python 脚本，目标文件是 `uk205.py`。

- 目标输出文件：`UK205_KP UEA转化漏斗概览.xlsx`
- 报表目的：按 KP 跟进人输出月度漏斗与节点取消分布
- 代码风格必须对齐现有正式脚本，输出结果要像 `uk101.py` 这种可直接落地的 Excel 生成脚本
- 必须生成完整代码，不要只输出思路、伪代码、占位函数或半成品
- 必须包含 `parse_args`、月度汇总、节点分布输出、`build_notes_rows`、`write_workbook`、`main`
- 必须默认自动识别对应基础表族的最新输入文件，并支持 `--input` / `--output`
- Excel 必须包含双表头、格式化列宽、冻结窗格、Notes sheet
- 下方各章节不是阅读说明，而是必须落地到代码中的业务规则与实现约束

## 1. 业务目标

从 `UK200` 基础表中，回答 **“不同 KP 跟进人的漏斗表现和取消分布如何”**。

## 2. 基础文件与数据粒度

- 基础文件：`UK200` 开头的转化漏斗 KPI 基础表
- 原始粒度：QPO 过程记录
- 输出粒度：`KP_Flup + Stat_Month`

## 3. 核心分析角度

1. **规模角度**：每位 KP 管理多少提报
2. **结果角度**：成功、取消、跟进中的结构
3. **问题定位角度**：取消集中在哪些节点

## 4. 关键维度

- `KP_Flup`
- `Stat_Month`
- `WPO_Current_Node`

## 5. 核心指标与口径

- `Total_QPO_Count`
- `Success_QPO_Count`
- `Cancel_QPO_Count`
- `InProgress_QPO_Count`
- `Success_QPO_Rate`
- `Cancel_QPO_Rate`
- `InProgress_QPO_Rate`
- 节点取消分布指标

## 6. 漏斗 / 阶段 / 状态逻辑

- 状态统一映射为 `Success / Cancel / InProgress`
- 取消节点要做统一归因

## 7. Excel 输出结构

- 主表：`KP_Funnel`
- Notes：字段定义、状态映射、节点口径

## 8. 实现提示

- `KP_Flup` 必填
- 默认只看 2025-01-01 之后提报
- 与 UK204 保持尽量一致的列结构，便于 OM/KP 横向比较

## 9. 代码复刻约束

以下内容直接从当前正式脚本反推，属于生成代码时应优先保持一致的实现约束。

- 默认输出文件名：`UK205_KP UEA转化漏斗概览.xlsx`
- 现有脚本实际创建的 Sheet：`UK205_KP UEA`, `Notes`
- 现有脚本核心函数：`parse_args`, `build_summary`, `write_main_sheet`, `write_notes_sheet`, `generate_report`, `main`
- `BASE_COLUMNS_EN` 必须保持以下顺序：`KP_Flup`, `Stat_Month`, `Total_QPO_Count`, `Success_QPO_Count`, `Success_QPO_Rate`, `Cancel_QPO_Count`, `Cancel_QPO_Rate`, `InProgress_QPO_Count`, `InProgress_QPO_Rate`
- `BASE_COLUMNS_CN` 必须保持以下顺序：`KP_Flup`, `统计月份`, `提报QPO总数`, `成交QPO数量`, `成交QPO占比`, `取消QPO数量`, `取消QPO占比`, `跟进中QPO数量`, `跟进中QPO占比`

## 10. Notes Sheet 输出要求（反推后沉淀为基准）

### 10.1 字段口径说明区

- 当前正式脚本的工作簿实际包含 `UK205_KP UEA` 与 `Notes` 两个 Sheet。
- `Notes` 当前不是表格字典风格，而是分段文本说明，至少包含：`输入文件`、`时间范围`、`口径说明`、`QPO_Status 状态映射规则`、`取消节点分布规则` 等段落。
- 文档里必须明确以下真实行为：
  - 默认只统计 `2025-01-01` 之后提报
  - `KP_Flup` 为空不参与统计
  - 主表包含 `ALL` 汇总
  - `Total = Success + Cancel + InProgress`，且三类结果均按 `QPO_ID` 去重统计
  - 取消节点分布仅针对 Cancel QPO，节点维度来自 `WPO_Current_Node`，中文名来自 `WPO_Current_Node.1`
  - 未映射状态会在 `Notes` 中列出并跳过统计。

### 10.2 运行元信息区

- 虽然当前 `Notes` 是分段文本，但文档基准里应明确其必须承载运行信息，至少包括：`Input_File`、`Date_Range`、`Output_Row_Count`、`All_Row_Count`、`Unmapped_Status_Breakdown`、`Detected_Node_List`。
- 重点不是做漂亮表格，而是保证后续重建脚本仍会把 KP 口径、状态映射和取消节点规则写进 `Notes`。

### 10.3 最终要求

- `UK205.md` 要保留当前正式脚本“文本型 Notes”的特征，避免后续生成时把 Notes 简化成空白说明页。
- 如果 `uk205.py` 的状态映射、节点归因或未映射状态处理方式变化，必须先更新本文件。

