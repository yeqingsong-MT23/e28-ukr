<!-- UKAI_METADATA
{"code":"UK2061","family":"UK200","script_name":"uk2061.py","file_prefixes":["UK2061"],"output_name":"UK2061_各订单等级三次报价转化漏斗概览.xlsx","purpose":"聚焦三次报价场景，按订单等级观察更深阶段的漏斗表现。","dimensions":["Month","Order_Level"],"metrics":["Third stage metrics","Success_QPO_Count","Cancel_QPO_Count","In_Progress_QPO_Count","Third_Stage_Success_Submit_Rate"],"filters":["基于 load_stage_qpo_records 的阶段结果","重点关注 Third 阶段","支持 ALL 汇总"],"source_fields":["Submission_Date","QPO_ID","QPO_Status","Order_Level","Initial_Quote","Second_Quote","MVM_Approved_Price"],"tags":["三次报价","Third阶段","订单等级","深阶段漏斗"]}
-->
# UK2061 代码生成提示词

## 0. Prompt / 给 uk_ai_brief.py 的指令

你现在要生成一个**实际落地、可直接运行、会输出 Excel 报表**的 Python 脚本，目标文件是 `uk2061.py`。

- 目标输出文件：`UK2061_各订单等级三次报价转化漏斗概览.xlsx`
- 报表目的：聚焦三次报价场景，按订单等级观察更深阶段的漏斗表现
- 代码风格必须对齐现有正式脚本，输出结果要像 `uk101.py` 这种可直接落地的 Excel 生成脚本
- 必须生成完整代码，不要只输出思路、伪代码、占位函数或半成品
- 必须包含 `parse_args`、阶段归并、专项 Summary 输出、`build_notes_rows`、`write_workbook`、`main`
- 必须默认自动识别对应基础表族的最新输入文件，并支持 `--input` / `--output`
- Excel 必须包含双表头、格式化列宽、冻结窗格、Notes sheet
- 下方各章节不是阅读说明，而是必须落地到代码中的业务规则与实现约束

## 1. 业务目标

这是 `UK206` 的专项版，聚焦回答 **“走到第三次报价的订单，按订单等级看最终转化质量如何”**。

## 2. 基础文件与数据粒度

- 基础文件：`UK200` 开头的转化漏斗 KPI 基础表
- 原始粒度：QPO 过程记录
- 输出粒度：`Month + Order_Level`

## 3. 核心分析角度

1. **深阶段规模角度**：走到 Third 阶段的 QPO 数
2. **深阶段结果角度**：Third 阶段后的成功、取消、跟进中
3. **订单等级角度**：不同等级在深阶段的表现

## 4. 关键维度

- `Month`
- `Order_Level`

## 5. 核心指标与口径

- `Third_Stage_QPO_Count`
- `Success_QPO_Count`
- `Cancel_QPO_Count`
- `In_Progress_QPO_Count`
- `Third_Stage_Success_Submit_Rate`

## 6. 漏斗 / 阶段 / 状态逻辑

- 基于与 `UK206` 一致的阶段归并逻辑
- 聚焦 `Third` 阶段相关记录与结果

## 7. Excel 输出结构

- 主表：`ThirdStage_Funnel`
- Notes：Third 阶段定义、指标说明

## 8. 实现提示

- 与 `UK206` 口径保持一致
- 只是在输出上更聚焦深阶段

## 9. 代码复刻约束

以下内容直接从当前正式脚本反推，属于生成代码时应优先保持一致的实现约束。

- 默认输出文件名：`UK2061_各订单等级三次报价转化漏斗概览.xlsx`
- 现有脚本实际创建的 Sheet：`UK2061`, `Notes`
- 现有脚本核心函数：`parse_args`, `aggregate`, `write_workbook`, `main`
- 固定订单等级顺序：`A`, `B`, `C`, `D`
- `ORDER_LEVELS` 必须保持以下顺序：`A`, `B`, `C`, `D`

## 10. Notes Sheet 输出要求（反推后沉淀为基准）

### 10.1 字段口径说明区

- 当前正式脚本的工作簿实际包含 `UK2061` 与 `Notes` 两个 Sheet。
- `Notes` 实际采用 6 列结构，并先写规则区、再写 `QA Report`：`Field (EN)` / `字段中文` / `Field Group` / `Purpose / Business Meaning` / `Calculation / Rule` / `Notes`。
- 文档中必须明确以下真实规则：
  - `Order_Level=AA` 归并为 `A`
  - 报价进入标记按“对应报价字段任一行非空即 True”
  - 阶段归属为 `Has_Third=True -> Third`，否则 `Has_Second=True -> Second`，否则 `Initial`
  - 异常整单剔除规则：若某 `QPO_ID` 被映射为 Success，但 `Has_Initial/Has_Second/Has_Third` 全 False，则整单剔除
  - 取消拆分只保留 `WootCancel` 与 `SellerCancel` 两类，并按优先级 `WootCancel > SellerCancel`
  - 汇总行 `Month=ALL` 时，Count 要重新汇总，Rate 也要重新按汇总分子/分母计算。
- 主表除整体漏斗列外，还必须保留三阶段的 `WootCancel / SellerCancel` 细分字段，不能只剩 `Third_Stage_QPO_Count` 一组简化列。

### 10.2 运行元信息区

- 当前 `Notes` 的 QA 区至少记录：`input_rows`、`valid_rows`、`excluded_qpo_ids` 数量与示例、`included_qpo_count`、`结果表行数`、`0分母情况`、`字段完整性检查`。
- 这一区块要支持回答：清洗前后规模、异常成功整单剔除了多少、最终纳入统计的去重 QPO 数是多少、字段数是否与预期一致。

### 10.3 最终要求

- `UK2061.md` 要把“Third 专项漏斗 + 规则区 + QA 区”一起沉淀为脚本重建基准。
- 若后续脚本改变异常剔除、取消拆分或汇总分母规则，必须优先回写本文件。

