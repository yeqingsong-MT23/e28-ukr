<!-- UKAI_METADATA
{"code":"UK201","family":"UK200","script_name":"uk201.py","file_prefixes":["UK201"],"output_name":"UK201_转化漏斗概览.xlsx","purpose":"按月汇总 QPO 的成交/取消/跟进中情况，并把取消归因到当前节点。","dimensions":["Month","WPO_Current_Node"],"metrics":["Total_QPO","Cancel_QPO","Deal_QPO","InProgress_QPO","Cancel_Rate","Deal_Rate","InProgress_Rate","<Node>_Cancel_QPO_Count","<Node>_Cancel_QPO_Rate"],"filters":["按 QPO_ID 去重","状态映射为 Deal/Cancel/InProgress","取消节点取同一 QPO 的最大 Nxxx 节点","补 ALL 汇总"],"source_fields":["Submission_Date","QPO_ID","QPO_Status","WPO_Current_Node"],"tags":["转化漏斗概览","取消节点归因","ALL汇总","月度漏斗"]}
-->
# UK201 代码生成提示词

## 0. Prompt / 给 uk_ai_brief.py 的指令

你现在要生成一个**实际落地、可直接运行、会输出 Excel 报表**的 Python 脚本，目标文件是 `uk201.py`。

- 目标输出文件：`UK201_转化漏斗概览.xlsx`
- 报表目的：按月汇总 QPO 的成交/取消/跟进中情况，并把取消归因到当前节点
- 代码风格必须对齐现有正式脚本，输出结果要像 `uk101.py` 这种可直接落地的 Excel 生成脚本
- 必须生成完整代码，不要只输出思路、伪代码、占位函数或半成品
- 必须包含 `parse_args`、清洗归并、Summary 输出、Notes 输出、`main`
- 必须默认自动识别对应基础表族的最新输入文件，并支持 `--input` / `--output`
- Excel 必须包含双表头、格式化列宽、冻结窗格、Notes sheet
- 下方各章节不是阅读说明，而是必须落地到代码中的业务规则与实现约束

## 1. 业务目标

从 `UK200` 基础表中，回答 **“每月 QPO 整体转化情况怎样，取消主要卡在哪个节点”**。

这是管理视角的总览漏斗，重点看总提报、成交、取消、跟进中，以及取消节点归因。

## 2. 基础文件与数据粒度

- 基础文件：`UK200` 开头的转化漏斗 KPI 基础表
- 原始粒度：QPO 过程记录
- 输出粒度：`Month`

## 3. 核心分析角度

1. **总量角度**：总提报、成交、取消、跟进中
2. **结果角度**：成交率、取消率、跟进中占比
3. **节点角度**：取消发生在哪个当前节点
4. **趋势角度**：月度变化与 ALL 汇总

## 4. 关键维度

- `Month`
- `WPO_Current_Node`

## 5. 核心指标与口径

- `Total_QPO`
- `Deal_QPO`
- `Cancel_QPO`
- `InProgress_QPO`
- `Deal_Rate`
- `Cancel_Rate`
- `InProgress_Rate`
- `<Node>_Cancel_QPO_Count`
- `<Node>_Cancel_QPO_Rate`

## 6. 漏斗 / 阶段 / 状态逻辑

- `QPO_Status` 统一映射为 `Deal / Cancel / InProgress`
- 对取消类 QPO，要识别其取消时对应的最大 `Nxxx` 节点
- 归因口径是“取消前实际推进到的最深节点”

## 7. Excel 输出结构

- 主表：`UK201_转化漏斗概览`
- Notes：状态映射、节点归因规则、自检说明

## 8. 实现提示

- 先按 `QPO_ID` 去重 / 归并，再做月度统计
- 必须补 `ALL` 汇总
- 节点列建议按标准节点顺序展开

## 9. 代码复刻约束

以下内容直接从当前正式脚本反推，属于生成代码时应优先保持一致的实现约束。

- 默认输出文件名：`UK201_转化漏斗概览.xlsx`
- 现有脚本实际创建的 Sheet：`UK201_转化漏斗概览`, `Notes`
- 现有脚本核心函数：`parse_args`, `parse_cell_date`, `normalize_text`, `has_chinese`, `find_latest_input`, `detect_data_sheet`, `resolve_column_indexes`, `map_status_category`, `normalize_node`, `node_attribution_rank`, `node_sort_key`, `month_sort_key`, `build_summary`, `build_column_headers`, `write_main_sheet`, `write_notes_sheet`, `generate_report`, `main`
- `BASE_COLUMNS_EN` 必须保持以下顺序：`Month`, `Total_QPO`, `Cancel_QPO`, `Deal_QPO`, `InProgress_QPO`, `Cancel_Rate`, `Deal_Rate`, `InProgress_Rate`
- `BASE_COLUMNS_CN` 必须保持以下顺序：`月份`, `提报QPO总数`, `取消QPO数`, `成交QPO数`, `跟进中QPO数`, `取消占比`, `成交占比`, `跟进中占比`

## 10. Notes Sheet 输出要求（反推后沉淀为基准）

### 10.1 字段口径说明区

- 当前正式脚本的工作簿实际包含 `UK201_转化漏斗概览` 与 `Notes` 两个 Sheet。
- `Notes` 当前是简化的 2 列结构：`Field` / `Definition/Rule`，前半段解释字段，后半段补 `Input/Processing Stats` 与 `Self-checks`。
- 除 `BASE_COLUMNS_EN` 外，主表还会在右侧动态展开 `<Node>_Cancel_QPO_Count` 与 `<Node>_Cancel_QPO_Rate` 一组取消节点列；文档里必须明确这是动态节点列，不是固定只到基础 8 列。
- 以下规则必须写死：
  - `Month` 由 `Submission_Date` 提取 `YYYY-MM`
  - `Total_QPO` 为 QPO 级排重后的 `QPO_ID` 数
  - `Cancel_QPO / Deal_QPO / InProgress_QPO` 基于最终映射类型统计
  - `<Node>_Cancel_QPO_Count` 对 Cancel 类 QPO 扫描同一 `QPO_ID` 全部源行，按最大 `Nxxx` 节点归因
  - `<Node>_Cancel_QPO_Rate` 的分母是同月 `Total_QPO`，不是 `Cancel_QPO`
  - `Other` 状态计入 `Total_QPO`，但不计入三大结果口径
  - 需要保留 `ALL` 总计行。

### 10.2 运行元信息区

- `Notes` 还要保留当前脚本已有的处理统计项，至少包括：`input_file`、`row_count_in`、`row_count_valid_after_date_parse`、`distinct_qpo_count`、`month_count`、`unmapped_status_qpo_count`、`missing_node_cn_rate`、`unknown_node_rate_in_cancel`。
- 如果存在未映射状态，还要记录 `unmapped_status_breakdown`。
- 自检区要能说明：节点中文缺失率、取消归因未知节点比例、以及主表合计是否通过。

### 10.3 最终要求

- `UK201.md` 必须把“动态取消节点列 + Notes 统计项”一起固化下来，否则后续重建容易只剩基础漏斗列。
- 若状态映射、节点归因或 `ALL` 汇总逻辑变化，必须先回写本文件。

