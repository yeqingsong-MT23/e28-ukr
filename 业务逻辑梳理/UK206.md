<!-- UKAI_METADATA
{"code":"UK206","family":"UK200","script_name":"uk206.py","file_prefixes":["UK206"],"output_name":"UK206｜各订单等级 × 报价阶段 转化漏斗.xlsx","purpose":"按订单等级拆分 QPO，并输出三段报价阶段的漏斗指标。","dimensions":["Month","Order_Level"],"metrics":["Submitted_QPO_Count","Success_QPO_Count","In_Progress_QPO_Count","Cancel_QPO_Count","Success_Rate","In_Progress_Rate","Cancel_Rate","Initial_Stage_QPO_Count","Second_Stage_QPO_Count","Third_Stage_QPO_Count"],"filters":["Order_Level 只保留 A/B/C/D","阶段按 Initial_Quote / Second_Quote / MVM_Approved_Price 是否存在推导","补 ALL 汇总"],"source_fields":["Submission_Date","QPO_ID","QPO_Status","Order_Level","Initial_Quote","Second_Quote","MVM_Approved_Price"],"tags":["订单等级漏斗","报价阶段","三阶段","ALL汇总","Order_Level"]}
-->
# UK206 代码生成提示词

## 0. Prompt / 给 uk_ai_brief.py 的指令

你现在要生成一个**实际落地、可直接运行、会输出 Excel 报表**的 Python 脚本，目标文件是 `uk206.py`。

- 目标输出文件：`UK206｜各订单等级 × 报价阶段 转化漏斗.xlsx`
- 报表目的：按订单等级拆分 QPO，并输出三段报价阶段的漏斗指标
- 代码风格必须对齐现有正式脚本，输出结果要像 `uk101.py` 这种可直接落地的 Excel 生成脚本
- 必须生成完整代码，不要只输出思路、伪代码、占位函数或半成品
- 必须包含 `parse_args`、阶段归并、Summary 输出、`build_notes_rows`、`write_workbook`、`main`
- 必须默认自动识别对应基础表族的最新输入文件，并支持 `--input` / `--output`
- Excel 必须包含双表头、格式化列宽、冻结窗格、Notes sheet
- 下方各章节不是阅读说明，而是必须落地到代码中的业务规则与实现约束

## 1. 业务目标

从 `UK200` 基础表中，回答 **“不同订单等级在不同报价深度下的转化表现是否不同”**。

重点看高价值订单是否更容易走到更深报价阶段，以及不同报价阶段的结果差异。

## 2. 基础文件与数据粒度

- 基础文件：`UK200` 开头的转化漏斗 KPI 基础表
- 原始粒度：QPO 过程记录
- 输出粒度：`Month + Order_Level`

## 3. 核心分析角度

1. **订单等级角度**：A/B/C/D 各自漏斗表现
2. **报价深度角度**：Initial / Second / Third
3. **结果角度**：成功、取消、跟进中

## 4. 关键维度

- `Month`
- `Order_Level`

## 5. 核心指标与口径

- `Submitted_QPO_Count`
- `Success_QPO_Count`
- `In_Progress_QPO_Count`
- `Cancel_QPO_Count`
- `Success_Rate`
- `In_Progress_Rate`
- `Cancel_Rate`
- `Initial_Stage_QPO_Count`
- `Second_Stage_QPO_Count`
- `Third_Stage_QPO_Count`

## 6. 漏斗 / 阶段 / 状态逻辑

- `Order_Level` 只保留 `A/B/C/D`
- 报价阶段由 `Initial_Quote / Second_Quote / MVM_Approved_Price` 是否存在推导
- 先形成 QPO 阶段结果，再按等级汇总

## 7. Excel 输出结构

- 主表：`UK206_OrderLevel_QuoteStageFunnel`
- Notes：阶段定义、等级口径、字段说明

## 8. 实现提示

- 必须补 `ALL` 汇总
- 阶段列建议固定顺序，便于不同等级横向比

## 9. 代码复刻约束

以下内容直接从当前正式脚本反推，属于生成代码时应优先保持一致的实现约束。

- 默认输出文件名：`UK206｜各订单等级 × 报价阶段 转化漏斗.xlsx`
- 主结果 Sheet 名：`UK206_OrderLevel_QuoteStageFunnel`
- 现有脚本实际创建的 Sheet：`UK206_OrderLevel_QuoteStageFunnel`, `Notes`
- 现有脚本核心函数：`parse_args`, `aggregate`, `sanitize_note_value`, `build_notes_rows`, `style_notes_sheet`, `write_workbook`, `main`
- 固定订单等级顺序：`A`, `B`, `C`, `D`
- `ORDER_LEVELS` 必须保持以下顺序：`A`, `B`, `C`, `D`

## 10. Notes Sheet 输出要求（反推后沉淀为基准）

### 10.1 字段口径说明区

- 当前正式脚本的工作簿实际包含 `UK206_OrderLevel_QuoteStageFunnel` 与 `Notes` 两个 Sheet。
- `Notes` 当前采用三列结构：`Item` / `Value` / `Description`。
- 至少要覆盖以下口径：`Month`、`Order_Level`、`Submitted_QPO_Count`、`Success_QPO_Count`、`In_Progress_QPO_Count`、`Cancel_QPO_Count`、`Success_Rate`、`In_Progress_Rate`、`Cancel_Rate`、以及三段报价阶段对应的 `*_Stage_QPO_Count / *_Stage_Success_QPO_Count / *_Stage_Success_Submit_Rate / *_Stage_Cancel_QPO_Count / *_Stage_Cancel_Submit_Rate`。
- 当前正式脚本的真实规则应写死：
  - 订单等级仅保留 `A/B/C/D`
  - `AA` 会并入 `A`
  - `Month` 使用 `month_dash` 口径，且存在 `ALL` 汇总行
  - 阶段归属按最高进入阶段判定：`Third > Second > Initial`
  - `Success_Rate / In_Progress_Rate / Cancel_Rate` 分母均为 `Submitted_QPO_Count`
  - 各阶段的 `*_Submit_Rate` 分母同样按当前脚本使用提报数口径，而不是阶段数平均。

### 10.2 运行元信息区

- 当前 `Notes` 至少要记录：`Report_Code`、`Input_File`、`Output_File`、`Main_Sheet`、`Notes_Sheet`、`Loaded_QPO_Count`、`Included_QPO_Count`、`Excluded_QPO_Count`、`Order_Level_List`、`Summary_Row_Count`、`Month_Range`、`Stage_Rule`、`Rate_Rule`。
- `load_stage_qpo_records` 返回的元信息需要先做可写入 Excel 的转换；像 `excluded_qpo_ids` 这类列表字段，必须转成字符串后再写入 `Notes`，不能直接把 Python 列表对象写进单元格。
- 这一块的目标是同时保留：
  - 本次运行用了哪份输入
  - 纳入 / 排除多少 QPO
  - 月份范围和阶段规则是什么
  - 附加元信息能否被 Excel 正常写出。

### 10.3 最终要求

- `UK206.md` 现在应以“主表 + Notes”作为完整正式输出基准，而不是只描述主表。
- 后续若 `uk206.py` 调整 Notes 结构、阶段分母、订单等级映射或元信息写法，必须优先更新本文件。

