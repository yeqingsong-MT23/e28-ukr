<!-- UKAI_METADATA
{"code":"UK102","family":"UK100","script_name":"uk102.py","file_prefixes":["UK102"],"output_name":"UK102_UEA_月度工作量价值结构_20260430.xlsx","purpose":"按月+部门+UEA 分析工作量的客户价值与订单价值结构。","dimensions":["Stat_Month","Department","UEA"],"metrics":["Total_Case_Cnt","DCM_VCP_Case","DCM_A_Case","DCM_B_Case","DCM_C_Case","KCP_Case","Non_KCP_Case","Order_A_Case","Order_B_Case","Order_C_Case","Order_D_Case","High_Value_Order_Case","Low_Value_Workload_Flag"],"filters":["Type in 工单类","Order_ID 和 seller Code 必填","DCM 空值默认归 C","订单等级按 A/B/C 或 D 归并"],"source_fields":["Stat_Date","Department","UEA","Type","Order_ID","seller Code","DCM","KCP","Order_Level"],"tags":["价值结构","DCM","KCP","订单等级","高价值订单","UEA"]}
-->
# UK102 代码生成提示词

## 0. Prompt / 给 uk_ai_brief.py 的指令

你现在要生成一个**实际落地、可直接运行、会输出 Excel 报表**的 Python 脚本，目标文件是 `uk102.py`。

- 目标输出文件：`UK102_UEA_月度工作量价值结构_20260430.xlsx`
- 报表目的：按月+部门+UEA 分析工作量的客户价值与订单价值结构
- 代码风格必须对齐现有正式脚本，输出结果要像 `uk101.py` 这种可直接落地的 Excel 生成脚本
- 必须生成完整代码，不要只输出思路、伪代码、占位函数或半成品
- 必须包含 `parse_args`、核心汇总函数、`build_notes_rows`、`write_workbook`、`main`
- 必须默认自动识别对应基础表族的最新输入文件，并支持 `--input` / `--output`
- Excel 必须包含双表头、格式化列宽、冻结窗格、Notes sheet
- 下方各章节不是阅读说明，而是必须落地到代码中的业务规则与实现约束

## 1. 业务目标

从 `UK100` 工作量基础表中，回答 **“每个处理人的工作量是否集中在高价值客户 / 高价值订单上”**。

重点不是总量，而是**价值结构**：

- 做了多少高价值订单
- 服务了多少高等级客户
- 是否在低价值工单上投入了过多精力

## 2. 基础文件与数据粒度

- 基础文件：`UK100` 开头的工作量 KPI 基础表
- 原始粒度：单条工作记录
- 输出粒度：`统计月份 + 部门 + UEA`

## 3. 核心分析角度

1. **客户价值角度**：DCM 分层、KCP / 非 KCP
2. **订单价值角度**：Order Level A/B/C/D
3. **高价值占比角度**：A/B/C 订单占比
4. **部门对比角度**：个人行 + 部门汇总行

## 4. 关键维度

- `Stat_Month`
- `Department`
- `UEA`

## 5. 核心指标与口径

- `Total_Case_Cnt`：工单类总量
- `DCM_VCP_Case / DCM_A_Case / DCM_B_Case / DCM_C_Case`
- `KCP_Case / Non_KCP_Case`
- `Order_A_Case / Order_B_Case / Order_C_Case / Order_D_Case`
- `High_Value_Order_Case`：A/B/C 订单之和
- `Low_Value_Workload_Flag`：高价值订单占比过低时的识别字段

## 6. 漏斗 / 阶段 / 状态逻辑

本表**没有漏斗**，是价值结构分析表。

## 7. Excel 输出结构

- 主表：`Result`
- Notes：字段定义、计算口径、价值结构说明
- 同时输出个人行和“部门汇总”行

## 8. 实现提示

- 只统计工单类，不统计邮件
- `Order_ID` 与 `seller Code` 必须有效
- DCM 需要归类；订单等级需要归并到 A/B/C/D

## 9. 代码复刻约束

以下内容直接从当前正式脚本反推，属于生成代码时应优先保持一致的实现约束。

- 默认输出文件名：`UK102_UEA_月度工作量价值结构_20260430.xlsx`
- 主结果 Sheet 名：`Result`
- 现有脚本实际创建的 Sheet：`Result`, `Notes`
- 现有脚本核心函数：`parse_args`, `normalize_dcm`, `normalize_order_group`, `build_summary`, `build_output_row`, `build_notes_rows`, `write_workbook`, `main`
- `COLUMNS_EN` 必须保持以下顺序：`Stat_Month`, `Department`, `UEA`, `Total_Case_Cnt`, `DCM_VCP_Case`, `DCM_VCP_Rate`, `DCM_A_Case`, `DCM_A_Rate`, `DCM_B_Case`, `DCM_B_Rate`, `DCM_C_Case`, `DCM_C_Rate`, `KCP_Case`, `KCP_Rate`, `Non_KCP_Case`, `Non_KCP_Rate`, `Order_A_Case`, `Order_A_Rate`, `Order_B_Case`, `Order_B_Rate`, `Order_C_Case`, `Order_C_Rate`, `Order_D_Case`, `Order_D_Rate`, `High_Value_Order_Case`, `High_Value_Order_Rate`, `Low_Value_Workload_Flag`
- `COLUMNS_CN` 必须保持以下顺序：`统计月份(YYYYMM)`, `部门`, `跟进人编号`, `工单总数（不去重）`, `DCM-VCP客户工单数`, `DCM-VCP客户占比`, `DCM-A客户工单数`, `DCM-A客户占比`, `DCM-B客户工单数`, `DCM-B客户占比`, `DCM-C客户工单数`, `DCM-C客户占比`, `KCP客户工单数`, `KCP客户占比`, `非KCP客户工单数`, `非KCP客户占比`, `A等级订单工单数`, `A等级订单占比`, `B等级订单工单数`, `B等级订单占比`, `C等级订单工单数`, `C等级订单占比`, `D等级订单工单数`, `D等级订单占比`, `高价值订单工单数(A/B/C)`, `高价值订单占比`, `是否低价值消耗（按订单）`

## 10. Notes Sheet 输出要求（反推后沉淀为基准）

### 10.1 字段口径说明区

- 当前正式脚本的工作簿实际包含 `Result` 与 `Notes` 两个 Sheet；不能把 `Notes` 误认为唯一输出。
- `Notes` 实际采用 4 列结构：`Field (EN)` / `字段（中文）` / `统计方式 / 计算口径` / `业务含义 / 管理解读`。
- 口径说明区至少要覆盖 `Stat_Month`、`Department`、`UEA`、`Total_Case_Cnt`、全部 DCM 字段、全部 KCP 字段、全部 Order Level 字段、`High_Value_Order_Case`、`High_Value_Order_Rate`、`Low_Value_Workload_Flag`。
- 以下规则必须明确保留：
  - 仅统计工单类记录，不统计邮件
  - `Order_ID` 与 `seller Code` 必填，否则不纳入统计
  - `Stat_Month` 输出口径为 `YYYYMM`，但排序逻辑需要兼容 `YYYYMM` 与 `YYYY-MM`
  - DCM 空值归 `C`
  - 订单等级按 `A/B/C/D` 归并，非 `A/B/C` 开头统一归 `D`
  - 所有 `*_Rate` 的分母均为 `Total_Case_Cnt`
  - `High_Value_Order_Case = Order_A_Case + Order_B_Case + Order_C_Case`
  - `Low_Value_Workload_Flag` 的阈值为 `High_Value_Order_Rate < 40%`
  - 输出中同时存在个人行与“部门汇总”行，`UEA=部门汇总` 不是脏值而是正式口径。

### 10.2 运行元信息区

- 当前正式脚本的 `Notes` 以字段口径为主，没有单独的大块 QA 区；但文档基准里应补上运行元信息要求，至少包括：`Report_Code`、`Input_File`、`Output_File`、`Data_Sheet`、`Loaded_Rows`、`Filtered_Rows`、`Output_Row_Count`、`Department_Summary_Row_Count`。
- 运行元信息重点记录：
  - 工单过滤前后行数
  - `Order_ID`/`seller Code` 缺失剔除规模
  - DCM 归类与订单等级归并是否生效
  - 输出中个人行与部门汇总行各有多少条。

### 10.3 最终要求

- `UK102.md` 要把“价值结构字段字典”作为重建脚本的硬约束，而不只是业务说明。
- 以后若 `Low_Value_Workload_Flag` 阈值、DCM 归类或部门汇总逻辑变化，必须优先更新本文件。

