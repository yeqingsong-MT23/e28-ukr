<!-- UKAI_METADATA
{"code":"UK208","family":"UK200","script_name":"uk208.py","file_prefixes":["UK208"],"output_name":"UK208_P5价格区间转化漏斗概览.xlsx","purpose":"按 P5 价格区间输出漏斗，并附带价格均值与清洗规则。","dimensions":["P5_Bucket","Stage"],"metrics":["Submitted_QPO_Count","Success_QPO_Count","Cancel_QPO_Count","In_Progress_QPO_Count","Avg_P5","Avg_P6","Avg_M1"],"filters":["按 P5 划分价格桶","包含两类异常删除逻辑","QPO 粒度归并后分析"],"source_fields":["Submission_Date","QPO_ID","QPO_Status","Child_Status","Initial_Quote","Second_Quote","MVM_Approved_Price","P5","P6","M1"],"tags":["P5价格区间","价格带漏斗","Avg_P5","Avg_P6","Avg_M1","清洗规则"]}
-->
# UK208 代码生成提示词

## 0. Prompt / 给 uk_ai_brief.py 的指令

你现在要生成一个**实际落地、可直接运行、会输出 Excel 报表**的 Python 脚本，目标文件是 `uk208.py`。

- 目标输出文件：`UK208_P5价格区间转化漏斗概览.xlsx`
- 报表目的：按 P5 价格区间输出漏斗，并附带价格均值与清洗规则
- 代码风格必须对齐现有正式脚本，输出结果要像 `uk101.py` 这种可直接落地的 Excel 生成脚本
- 必须生成完整代码，不要只输出思路、伪代码、占位函数或半成品
- 必须包含 `parse_args`、价格带清洗、Summary 输出、`build_notes_rows`、`write_workbook`、`main`
- 必须默认自动识别对应基础表族的最新输入文件，并支持 `--input` / `--output`
- Excel 必须包含双表头、格式化列宽、冻结窗格、Notes sheet
- 下方各章节不是阅读说明，而是必须落地到代码中的业务规则与实现约束

## 1. 业务目标

从 `UK200` 基础表中，回答 **“不同 P5 价格带的漏斗表现是否不同”**。

同时给出价格均值，便于判断高价带 / 低价带的转化差异。

## 2. 基础文件与数据粒度

- 基础文件：`UK200` 开头的转化漏斗 KPI 基础表
- 原始粒度：QPO 过程记录
- 输出粒度：`P5_Bucket + Stage`

## 3. 核心分析角度

1. **价格带角度**：不同 P5 区间的漏斗差异
2. **结果角度**：成功、取消、跟进中
3. **价格水平角度**：P5/P6/M1 平均值

## 4. 关键维度

- `P5_Bucket`
- `Stage`

## 5. 核心指标与口径

- `Submitted_QPO_Count`
- `Success_QPO_Count`
- `Cancel_QPO_Count`
- `In_Progress_QPO_Count`
- `Avg_P5`
- `Avg_P6`
- `Avg_M1`

## 6. 漏斗 / 阶段 / 状态逻辑

- 先按 QPO 粒度归并
- 再按 P5 划分价格桶
- 漏斗状态仍用统一成功 / 取消 / 跟进中口径

## 7. Excel 输出结构

- 主表：`P5Bucket_Funnel`
- Notes / Audit：清洗规则、删行说明、价格桶定义

## 8. 实现提示

- 需要执行异常删除逻辑
- 价格字段必须做数值清洗
- 价格桶边界要固定写入 Notes 里

## 9. 代码复刻约束

以下内容直接从当前正式脚本反推，属于生成代码时应优先保持一致的实现约束。

- 默认输出文件名：`UK208_P5价格区间转化漏斗概览.xlsx`
- 现有脚本实际创建的 Sheet：`UK208`, `Notes`
- 现有脚本核心函数：`parse_args`, `to_number`, `bucket_p5`, `map_split_type`, `build_clean_records`, `aggregate`, `write_workbook`, `main`
- 固定价格桶顺序：`$0-$20`, `$20-$35`, `$35-$50`, `$50-$80`, `$80-$120`, `$120-$200`, `$200-$350`, `$350+`
- `BUCKETS` 必须保持以下顺序：`$0-$20`, `$20-$35`, `$35-$50`, `$50-$80`, `$80-$120`, `$120-$200`, `$200-$350`, `$350+`

## 10. Notes Sheet 输出要求（反推后沉淀为基准）

### 10.1 字段口径说明区

- 当前正式脚本的工作簿实际包含 `UK208` 与 `Notes` 两个 Sheet。
- `Notes` 实际采用 5 列结构：`Field_English` / `Field_Chinese` / `Field_Group` / `Definition` / `Denominator/Notes`。
- 字段口径区至少要覆盖：`P5_Bucket`、`Submitted_QPOs`、`Success_QPOs`、`Success_Rate`、`Cancel_QPOs`、`Cancel_Rate`、`InProgress_QPOs`、`WootCancel_QPOs`、`SellerCancel_QPOs`、`Avg_P5_Success`、`Avg_P6_Success`、`Avg_M1_Success`、`M1_over_P6`，以及三阶段的 `Quoted / Success / Cancel / InProgress / WootCancel / SellerCancel` 相关字段。
- 以下规则必须明确写死：
  - `P5_Bucket` 取 QPO 粒度 `Avg_P5` 后再分桶，而不是逐行分桶
  - 分桶边界固定为左闭右开：`[0,20) / [20,35) / [35,50) / [50,80) / [80,120) / [120,200) / [200,350) / [350,∞)`
  - `Avg_P5` 缺失或 `<0` 的 QPO 不进入任何桶
  - `M1_over_P6 = Avg_M1_Success / Avg_P6_Success`
  - 阶段内 `Success/Cancel/InProgress_Rate` 分母是对应阶段 `Quoted_QPOs`
  - 总体取消拆分口径与阶段取消拆分口径要分开写明。

### 10.2 运行元信息区

- 当前 `Notes` 已包含 Audit 项，至少要保留：`Anomaly Deletion - Type A`、`Anomaly Deletion - Type B`、`Post-clean Unique QPOs`、`Bucketed Submitted Sum`、`Unbucketed QPOs (missing Avg_P5)`。
- 这一区块必须能回答：
  - A 类异常子行删了多少
  - B 类异常整单剔除了多少 `QPO_ID`
  - 清洗后去重 QPO 数是多少
  - 桶内提报数求和与最终纳入统计数是否一致
  - 有多少 QPO 因 `Avg_P5` 缺失或异常未分桶。

### 10.3 最终要求

- `UK208.md` 必须把“分桶边界 + 清洗规则 + Audit 区”固定下来，而不是只描述价格带概念。
- 如果 `uk208.py` 的异常删除、分桶边界或均值口径变化，必须优先回写本文件。

