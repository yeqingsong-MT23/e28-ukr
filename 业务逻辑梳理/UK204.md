<!-- UKAI_METADATA
{"code":"UK204","family":"UK200","script_name":"uk204.py","file_prefixes":["UK204"],"output_name":"UK204_OM UEA转化漏斗概览.xlsx","purpose":"按 OM 跟进人输出月度漏斗与节点分布。","dimensions":["OM_Flup","Stat_Month"],"metrics":["Total_QPO_Count","Success_QPO_Count","Cancel_QPO_Count","InProgress_QPO_Count","Success_QPO_Rate","Cancel_QPO_Rate","InProgress_QPO_Rate","Node distribution"],"filters":["OM_Flup 必填","默认仅统计 2025-01-01 之后","支持 ALL 汇总"],"source_fields":["Submission_Date","QPO_ID","QPO_Status","OM_Flup","WPO_Current_Node"],"tags":["OM漏斗","跟进人","节点分布","月度汇总"]}
-->
# UK204 代码生成提示词

## 0. Prompt / 给 uk_ai_brief.py 的指令

你现在要生成一个**实际落地、可直接运行、会输出 Excel 报表**的 Python 脚本，目标文件是 `uk204.py`。

- 目标输出文件：`UK204_OM UEA转化漏斗概览.xlsx`
- 报表目的：按 OM 跟进人输出月度漏斗与节点分布
- 代码风格必须对齐现有正式脚本，输出结果要像 `uk101.py` 这种可直接落地的 Excel 生成脚本
- 必须生成完整代码，不要只输出思路、伪代码、占位函数或半成品
- 必须包含 `parse_args`、月度汇总、节点分布输出、`build_notes_rows`、`write_workbook`、`main`
- 必须默认自动识别对应基础表族的最新输入文件，并支持 `--input` / `--output`
- Excel 必须包含双表头、格式化列宽、冻结窗格、Notes sheet
- 下方各章节不是阅读说明，而是必须落地到代码中的业务规则与实现约束

## 1. 业务目标

从 `UK200` 基础表中，回答 **“不同 OM 跟进人的漏斗表现有何差异”**。

重点用于看 OM 负责范围内的提报规模、转化率，以及取消更集中在哪些节点。

## 2. 基础文件与数据粒度

- 基础文件：`UK200` 开头的转化漏斗 KPI 基础表
- 原始粒度：QPO 过程记录
- 输出粒度：`OM_Flup + Stat_Month`

## 3. 核心分析角度

1. **人效规模角度**：每位 OM 跟进多少 QPO
2. **结果角度**：成功、取消、跟进中
3. **节点角度**：取消更多发生在哪些节点
4. **趋势角度**：月度跟进表现变化

## 4. 关键维度

- `OM_Flup`
- `Stat_Month`
- `WPO_Current_Node`

## 5. 核心指标与口径

- `Total_QPO_Count`
- `Success_QPO_Count`
- `Cancel_QPO_Count`
- `InProgress_QPO_Count`
- `Success_QPO_Rate`
- `Cancel_QPO_Rate`
- `InProgress_QPO_Rate`
- 节点取消分布指标

## 6. 漏斗 / 阶段 / 状态逻辑

- 状态统一映射为 `Success / Cancel / InProgress`
- 取消节点按最终停留节点或最大 `Nxxx` 节点归因

## 7. Excel 输出结构

- 主表：`OM_Funnel`
- Notes：状态映射、节点口径、筛选条件

## 8. 实现提示

- `OM_Flup` 必填
- 默认只看 2025-01-01 之后的记录
- 可补 `ALL` 汇总，方便整体对比

## 9. 代码复刻约束

以下内容直接从当前正式脚本反推，属于生成代码时应优先保持一致的实现约束。

- 默认输出文件名：`UK204_OM UEA转化漏斗概览.xlsx`
- 现有脚本实际创建的 Sheet：`UK204_OM UEA`, `Notes`
- 现有脚本核心函数：`parse_args`, `parse_cli_date`, `parse_cell_date`, `has_chinese`, `normalize_text`, `find_latest_input`, `map_status_category`, `node_sort_key`, `month_sort_key`, `detect_data_sheet`, `resolve_column_indexes`, `build_summary`, `build_column_headers`, `write_main_sheet`, `write_notes_sheet`, `generate_report`, `main`
- `BASE_COLUMNS_EN` 必须保持以下顺序：`OM_Flup`, `Stat_Month`, `Total_QPO_Count`, `Success_QPO_Count`, `Success_QPO_Rate`, `Cancel_QPO_Count`, `Cancel_QPO_Rate`, `InProgress_QPO_Count`, `InProgress_QPO_Rate`
- `BASE_COLUMNS_CN` 必须保持以下顺序：`OM跟进人`, `统计月份`, `提报QPO总数`, `成交QPO数量`, `成交QPO占比`, `取消QPO数量`, `取消QPO占比`, `跟进中QPO数量`, `跟进中QPO占比`

## 10. Notes Sheet 输出要求（反推后沉淀为基准）

### 10.1 字段口径说明区

- 当前正式脚本的工作簿实际包含 `UK204_OM UEA` 与 `Notes` 两个 Sheet。
- `Notes` 实际采用 6 列结构：`Field_EN` / `Field_CN` / `Field_Group` / `Definition` / `Enum_or_Notes` / `Sheet`。
- 主表除 `BASE_COLUMNS_EN` 外，还会动态展开取消节点分布列；文档里必须明确节点列来自运行期识别的 `nodes_sorted`，不是固定写死若干列。
- 以下规则必须明确：
  - 默认只统计 `Submission_Date >= 2025-01-01`
  - `OM_Flup` 为空不参与汇总
  - `Month` 从提报日期提取，且存在 `ALL` 汇总行
  - `QPO_Status` 统一映射到 `Success / Cancel / InProgress`
  - 取消节点归因使用当前脚本识别到的节点代码及节点中文映射
  - 汇总行的 Rate 需由汇总分子/分母重新计算，不能简单平均。

### 10.2 运行元信息区

- 当前 `Notes` 已含 `SELF_CHECK_REPORT` 区，至少要保留：`Input_Rows`、`Valid_Date_Rows`、`Start_Date_Filter`、`Missing_Node_CN`、`validation_total_equals_sum` 等自检信息。
- 该区块要能说明：
  - 起始日期过滤前后规模
  - 节点中文是否缺失
  - `Total = Success + Cancel + InProgress` 是否通过校验
  - 当前纳入的节点列表与节点中文映射是否完整。

### 10.3 最终要求

- `UK204.md` 需要把“OM 月度漏斗 + 动态节点分布 + Self-Check”作为完整输出定义。
- 若日期过滤、状态映射、节点列展开或自检规则变化，必须优先回写本文件。

