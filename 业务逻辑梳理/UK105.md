<!-- UKAI_METADATA
{"code":"UK105","family":"UK100","script_name":"uk105.py","file_prefixes":["UK105"],"output_name":"UK105_UEA处理人邮件处理时效分析.xlsx","purpose":"按处理人+邮件节点统计邮件数量、总处理时长和平均时长。","dimensions":["UEA","Metric_Code","Metric_Name"],"metrics":["Email_Count","Total_Handle_Time_Min","Avg_Handle_Time_Min"],"filters":["Type=邮件","UEA/Metric_Code/Metric_Name 必填","同时输出 All 汇总行"],"source_fields":["Type","UEA","Metric_Code","Metric_Name","Handle_Time_Minutes"],"tags":["邮件时效","处理人","邮件节点","平均时长","All汇总"]}
-->
# UK105 代码生成提示词

## 0. Prompt / 给 uk_ai_brief.py 的指令

你现在要生成一个**实际落地、可直接运行、会输出 Excel 报表**的 Python 脚本，目标文件是 `uk105.py`。

- 目标输出文件：`UK105_UEA处理人邮件处理时效分析.xlsx`
- 报表目的：按处理人+邮件节点统计邮件数量、总处理时长和平均时长
- 代码风格必须对齐现有正式脚本，输出结果要像 `uk101.py` 这种可直接落地的 Excel 生成脚本
- 必须生成完整代码，不要只输出思路、伪代码、占位函数或半成品
- 必须包含 `parse_args`、核心汇总函数、`build_notes_rows`、`write_workbook`、`main`
- 必须默认自动识别对应基础表族的最新输入文件，并支持 `--input` / `--output`
- Excel 必须包含双表头、格式化列宽、冻结窗格、Notes sheet
- 下方各章节不是阅读说明，而是必须落地到代码中的业务规则与实现约束

## 1. 业务目标

从 `UK100` 工作量基础表中，回答 **“不同处理人在不同邮件节点上的处理时效如何”**。

## 2. 基础文件与数据粒度

- 基础文件：`UK100` 开头的工作量 KPI 基础表
- 原始粒度：单条邮件处理记录
- 输出粒度：`UEA + Metric_Code + Metric_Name`

## 3. 核心分析角度

1. **数量角度**：处理了多少封邮件
2. **总时长角度**：累计花了多少分钟
3. **平均时长角度**：平均每封邮件处理多久
4. **整体对比角度**：All 汇总行

## 4. 关键维度

- `UEA`
- `Metric_Code`
- `Metric_Name`

## 5. 核心指标与口径

- `Email_Count`
- `Total_Handle_Time_Min`
- `Avg_Handle_Time_Min`

## 6. 漏斗 / 阶段 / 状态逻辑

本表**没有漏斗**，是邮件时效分析表。

## 7. Excel 输出结构

- 主表：`UK105_Result`
- Notes：字段说明、All 行说明、数值格式

## 8. 实现提示

- 只统计 `Type=邮件`
- 同时生成 `All` 汇总行
- 平均处理时长保留两位小数

## 9. 代码复刻约束

以下内容直接从当前正式脚本反推，属于生成代码时应优先保持一致的实现约束。

- 默认输出文件名：`UK105_UEA处理人邮件处理时效分析.xlsx`
- 主结果 Sheet 名：`UK105_Result`
- 现有脚本实际创建的 Sheet：`Notes`
- 现有脚本核心函数：`parse_args`, `build_summary`, `build_notes_rows`, `write_workbook`, `main`
- `COLUMNS_EN` 必须保持以下顺序：`UEA`, `Metric_Code`, `Metric_Name`, `Email_Count`, `Total_Handle_Time_Min`, `Avg_Handle_Time_Min`
- `COLUMNS_CN` 必须保持以下顺序：`处理人`, `邮件类型编码`, `邮件节点名称`, `处理数量`, `总处理时长（分钟）`, `平均处理时长（分钟）`

## 10. Notes Sheet 输出要求（反推后沉淀为基准）

### 10.1 字段口径说明区

- 当前正式脚本的工作簿实际包含 `UK105_Result` 与 `Notes` 两个 Sheet。
- `Notes` 实际采用 3 列结构：`Field` / `中文名` / `解释和含义`，后半段继续补充“口径说明”。
- 字段区至少要覆盖：`UEA`、`Metric_Code`、`Metric_Name`、`Email_Count`、`Total_Handle_Time_Min`、`Avg_Handle_Time_Min`。
- 需要明确以下真实行为：
  - 仅统计 `Type=邮件`
  - 输出粒度是 `UEA + Metric_Code + Metric_Name`
  - 同一 `Metric_Code + Metric_Name` 下，脚本会同时输出 `UEA=All` 的汇总行
  - `All` 行在每个节点组内排序靠前
  - `Email_Count` 按行为记录计数，不按 `Ticket_No` 去重
  - `Avg_Handle_Time_Min = Total_Handle_Time_Min / Email_Count`
  - 平均处理时长保留两位小数。

### 10.2 运行元信息区

- 当前 `Notes` 虽未单列 QA 表，但文档基准里应补足运行元信息，至少记录：`Input_File`、`Output_File`、`Loaded_Rows`、`Email_Rows`、`Skipped_Missing_UEA_Rows`、`Skipped_Missing_Metric_Code_Rows`、`Skipped_Missing_Metric_Name_Rows`、`Skipped_Bad_Handle_Time_Rows`、`Output_Row_Count`、`All_Row_Count`。
- 该区块的作用是让后续重建脚本时，可以追踪邮件过滤、时长解析失败和 `All` 汇总生成情况。

### 10.3 最终要求

- `UK105.md` 应把 `All` 行规则与“邮件按行为计数、不去重”写成硬口径。
- 以后如果 `uk105.py` 的排序、时长解析或 `All` 汇总方式调整，必须先更新这里。

