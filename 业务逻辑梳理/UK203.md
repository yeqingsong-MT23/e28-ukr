<!-- UKAI_METADATA
{"code":"UK203","family":"UK200","script_name":"uk203.py","file_prefixes":["UK203"],"output_name":"UK203_Seller转化漏斗概览.xlsx","purpose":"按卖家输出月度漏斗与历史累计漏斗。","dimensions":["Seller_ID","Submit_Month"],"metrics":["Total_QPO_Count","Deal_QPO_Count","Cancel_QPO_Count","InProgress_QPO_Count","Deal_QPO_Rate","Cancel_QPO_Rate","InProgress_QPO_Rate","First_Submit_Date","Last_Submit_Date"],"filters":["Seller_ID 必填","月度部分默认从 2025-01-01 起","累计部分默认从 2020-01-01 起"],"source_fields":["Submission_Date","QPO_ID","QPO_Status","Seller_ID","WPO_Current_Node"],"tags":["Seller漏斗","月度+累计","卖家维度","首末提报日期"]}
-->
# UK203 代码生成提示词

## 0. Prompt / 给 uk_ai_brief.py 的指令

你现在要生成一个**实际落地、可直接运行、会输出 Excel 报表**的 Python 脚本，目标文件是 `uk203.py`。

- 目标输出文件：`UK203_Seller转化漏斗概览.xlsx`
- 报表目的：按卖家输出月度漏斗与历史累计漏斗
- 代码风格必须对齐现有正式脚本，输出结果要像 `uk101.py` 这种可直接落地的 Excel 生成脚本
- 必须生成完整代码，不要只输出思路、伪代码、占位函数或半成品
- 必须包含 `parse_args`、月度与累计双视角输出、`build_notes_rows`、`write_workbook`、`main`
- 必须默认自动识别对应基础表族的最新输入文件，并支持 `--input` / `--output`
- Excel 必须包含双表头、格式化列宽、冻结窗格、Notes sheet
- 下方各章节不是阅读说明，而是必须落地到代码中的业务规则与实现约束

## 1. 业务目标

从 `UK200` 基础表中，回答 **“不同 Seller 的历史提报规模和转化表现如何”**。

既看月度表现，也看 Seller 历史累计表现，用于识别重点卖家、低转化卖家、长期跟进卖家。

## 2. 基础文件与数据粒度

- 基础文件：`UK200` 开头的转化漏斗 KPI 基础表
- 原始粒度：QPO 过程记录
- 输出粒度：
  - 月度层：`Seller_ID + Submit_Month`
  - 累计层：`Seller_ID`

## 3. 核心分析角度

1. **卖家规模角度**：该卖家提报过多少 QPO
2. **卖家结果角度**：成交、取消、跟进中分布
3. **生命周期角度**：首提报日期、末提报日期
4. **横向对比角度**：卖家之间转化率对比

## 4. 关键维度

- `Seller_ID`
- `Submit_Month`

## 5. 核心指标与口径

- `Total_QPO_Count`
- `Deal_QPO_Count`
- `Cancel_QPO_Count`
- `InProgress_QPO_Count`
- `Deal_QPO_Rate`
- `Cancel_QPO_Rate`
- `InProgress_QPO_Rate`
- `First_Submit_Date`
- `Last_Submit_Date`

## 6. 漏斗 / 阶段 / 状态逻辑

- 状态统一映射为 `Deal / Cancel / InProgress`
- 先按 QPO 归并结果，再汇总到 Seller

## 7. Excel 输出结构

- `Monthly_Seller`
- `Accumulated_Seller`
- `Notes`

## 8. 实现提示

- `Seller_ID` 为空的记录不参与 Seller 视图
- 月度部分与累计部分要分别建表，避免混在一起
- 建议保留卖家首末提报日期方便业务追踪

## 9. 代码复刻约束

以下内容直接从当前正式脚本反推，属于生成代码时应优先保持一致的实现约束。

- 默认输出文件名：`UK203_Seller转化漏斗概览.xlsx`
- 现有脚本实际创建的 Sheet：`UK203_Seller_Month`, `UK203_Seller`, `Notes`
- 现有脚本核心函数：`parse_args`, `parse_cell_date`, `normalize_text`, `has_chinese`, `find_latest_input`, `detect_data_sheet`, `resolve_column_indexes`, `map_status_category`, `normalize_node`, `node_rank`, `node_sort_key`, `month_sort_key`, `render_distribution`, `render_value_or_none`, `build_node_columns`, `build_summary`, `build_month_rows`, `build_seller_rows`, `apply_sheet_style`, `write_month_sheet`, `write_seller_sheet`, `write_notes_sheet`, `generate_report`, `main`
- 固定状态优先级：`{"Other": 0, "InProgress": 1, "Deal": 2, "Cancel": 3}`
- `MONTH_BASE_COLUMNS_EN` 必须保持以下顺序：`Seller_ID`, `Submit_Month`, `Total_QPO_Count`, `Deal_QPO_Count`, `Cancel_QPO_Count`, `InProgress_QPO_Count`, `Deal_QPO_Rate`, `Cancel_QPO_Rate`, `InProgress_QPO_Rate`
- `MONTH_BASE_COLUMNS_CN` 必须保持以下顺序：`卖家ID`, `提报月份`, `当月提报QPO总数`, `当月成交QPO数量`, `当月取消QPO数量`, `当月跟进中QPO数量`, `当月成交占比`, `当月取消占比`, `当月跟进中占比`
- `SELLER_BASE_COLUMNS_EN` 必须保持以下顺序：`Seller_ID`, `Total_QPO_Count`, `Deal_QPO_Count`, `Cancel_QPO_Count`, `InProgress_QPO_Count`, `Deal_QPO_Rate`, `Cancel_QPO_Rate`, `InProgress_QPO_Rate`, `First_Submit_Date`, `Last_Submit_Date`
- `SELLER_BASE_COLUMNS_CN` 必须保持以下顺序：`卖家ID`, `2020至今提报总数`, `2020至今成交QPO数量`, `2020至今取消QPO数量`, `2020至今跟进中QPO数量`, `2020至今成交占比`, `2020至今取消占比`, `2020至今跟进中占比`, `最早提报日期`, `最近提报日期`

## 10. Notes Sheet 输出要求（反推后沉淀为基准）

### 10.1 字段口径说明区

- 当前正式脚本的工作簿实际包含 `UK203_Seller_Month`、`UK203_Seller`、`Notes` 三个 Sheet。
- `Notes` 实际采用 7 列结构：`Field (EN)` / `Field (CN)` / `Sheet` / `Field Group` / `Business Meaning` / `Calculation / Rule` / `Notes`。
- 文档中必须明确两套结果视图：
  - `UK203_Seller_Month`：`Seller_ID + Submit_Month`，默认只保留 `2025-01-01` 及以后数据
  - `UK203_Seller`：`Seller_ID` 累计视图，默认统计 `2020-01-01` 及以后数据。
- 以下规则必须写死：
  - `Seller_ID` 为空的记录不进入 Seller 视图
  - QPO 统计采用 `Seller + QPO` 组合去重
  - 状态优先级固定为 `Other < InProgress < Deal < Cancel`
  - 月度与累计两张表都带动态节点列，节点由 `build_node_columns` 按实际节点展开
  - 累计表需要保留 `First_Submit_Date` 与 `Last_Submit_Date`。

### 10.2 运行元信息区

- 当前 `Notes` 已经包含较完整 QA 区，至少要保留：`Input_File`、`UK200_Raw_Rows`、`Dedup_QPO_Count`、`Dedup_Date_Min`、`Dedup_Date_Max`、`QPO_Count_2025_plus`、`QPO_Count_2020_plus`、`Other_Status_Distribution`、`Missing_Node_CN`。
- 这一块要能区分：
  - 原始有效行与排重后 QPO 数
  - 月度范围与累计范围各自纳入了多少 QPO
  - 未映射状态分布
  - 节点中文缺失清单。

### 10.3 最终要求

- `UK203.md` 必须把“双视图 + 动态节点列 + QA 区”视为正式输出基准。
- 后续若月度起始日期、Seller 过滤、排重键或节点展开逻辑变化，必须先同步本文件。

