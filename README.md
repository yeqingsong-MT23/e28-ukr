# e28-ukr

This repository contains UK reporting scripts, business logic notes, and helper command files.

## Smoke test notes

- `codex/test-pr` is the working branch for GitHub workflow smoke testing.
- Excel exports are intentionally excluded from version control.
- A minimal CI workflow validates that all Python files compile.
